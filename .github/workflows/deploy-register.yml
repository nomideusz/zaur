name: Deploy register to CapRover

on:
  push:
    branches: [main]
    paths:
      - 'apps/register/**'
      - 'packages/ui/**'
      - 'pnpm-lock.yaml'
      - 'pnpm-workspace.yaml'
      - 'package.json'
      - '.npmrc'
      - 'infra/deploy/register.captain-definition'
      - '.github/workflows/deploy-register.yml'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Prepare captain-definition
        run: cp infra/deploy/register.captain-definition captain-definition

      - name: Create deployment tarball
        uses: a7ul/tar-action@v1.1.0
        with:
          command: c
          cwd: ./
          files: |
            captain-definition
            pnpm-lock.yaml
            pnpm-workspace.yaml
            package.json
            .npmrc
            packages/ui/
            apps/register/
          outPath: deploy.tar

      - name: Deploy to CapRover
        uses: caprover/deploy-from-github@v1.1.2
        with:
          server: https://captain.zaur.app
          app: register
          token: ${{ secrets.CAPROVER_REGISTER_TOKEN }}
