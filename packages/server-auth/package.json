{
	"name": "@zaur/server-auth",
	"version": "0.1.0",
	"private": true,
	"description": "Server-side session store, sealing, and Stalwart OAuth plumbing shared by webmail and Mail 2.0 (extracted per ADR-0005)",
	"type": "module",
	"exports": {
		".": "./src/index.ts",
		"./*": "./src/*.ts"
	},
	"scripts": {
		"check": "tsc --noEmit",
		"test": "node --test --experimental-strip-types tests/*.test.ts"
	},
	"dependencies": {
		"@sveltejs/kit": "^2.57.0",
		"@zaur/mail-core": "workspace:*"
	},
	"devDependencies": {
		"typescript": "^5.6.0",
		"@types/node": "^25.9.1"
	}
}
