<script lang="ts">
	/**
	 * Legacy floating-island compose dock — unused. Compose actions live in
	 * MobileTopBar (Send) + the compose footer (single Attach). Kept so older
	 * imports/labs do not break; MobileIsland hides compose mode entirely.
	 */
</script>
