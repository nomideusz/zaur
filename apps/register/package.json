{
  "name": "@zaur/register",
  "version": "1.0.0",
  "description": "Self-service email registration portal for Stalwart mail",
  "main": "server.js",
  "scripts": {
    "predev": "pnpm --filter @zaur/ui build:css",
    "prestart": "pnpm --filter @zaur/ui build:css",
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test:e2e": "playwright test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@zaur/ui": "workspace:*",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "express-session": "^1.18.1",
    "nodemailer": "^6.10.1",
    "session-file-store": "^1.5.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
